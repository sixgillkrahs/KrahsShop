server:
  port: 5001

spring:
  application:
    name: MANUFACTURER-SERVICE

  jpa:
    hibernate:
      ddl-auto: update

    properties:
      hibernate, dialect: org.hibernate.dialect.PostgreSQLDialect

  datasource:
    url: jdbc:postgresql://localhost:5432/manufacturer
    username: postgres
    password: 123456